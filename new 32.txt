import React, { useState, useEffect } from "react";
import axiosInstance from "__api__/axiosInstance";
import API_CONFIG from "__api__/config";
import {
  Box,
  Tabs,
  Tab,
  Card,
  CardContent,
  Grid,
  TextField,
  Select,
  MenuItem,
  InputLabel,
  FormControl,
  Button,
  Table,
  TableBody,
  TableCell,
  TableContainer,
  TableHead,
  TableRow,
  Paper,
  Typography,
  CircularProgress,
  IconButton,
} from "@mui/material";
import { LocalizationProvider, DateTimePicker } from "@mui/x-date-pickers";
import { AdapterDateFns } from "@mui/x-date-pickers/AdapterDateFns";
import TrainIcon from "@mui/icons-material/Train";
import LocalShippingIcon from "@mui/icons-material/LocalShipping";
import InventoryIcon from "@mui/icons-material/Inventory";
import BuildIcon from "@mui/icons-material/Build";
import PeopleIcon from "@mui/icons-material/People";
import WaterIcon from "@mui/icons-material/Water";
import SaveIcon from "@mui/icons-material/Save";
import CancelIcon from "@mui/icons-material/Cancel";
import { useParams, useNavigate } from "react-router-dom";
import { format } from "date-fns";

// Normalize API data to ensure consistent value formats
const normalizeDropdownData = (data) => {
  if (!Array.isArray(data)) return [];
  return data.map((item, index) => ({
    id: item.id || `item-${index}`,
    name: item.name || item,
  }));
};

const Discharge = () => {
  const { portOperationsId } = useParams();
  const navigate = useNavigate();

  const [parties, setParties] = useState([]);
  const [cargoList, setCargoList] = useState([]);
  const [bays, setBays] = useState([]);
  const [berths, setBerths] = useState([]);
  const [shifts, setShifts] = useState([]);
  const [machineries, setMachineries] = useState([]);
  const [suppliers, setSuppliers] = useState([]);
  const [detailsTab, setDetailsTab] = useState(0);
  const [stockpileRows, setStockpileRows] = useState([]);
  const [isLoading, setIsLoading] = useState(false);
  const [vesselDetails, setVesselDetails] = useState({
    vesselName: "",
    voyageNumber: "",
    cargo: "",
    loadPort: "",
    dischargePort: "",
    customer: "",
    contract: "",
    vesselLoadQuantity: "",
    berthingTime: null,
    sailingTime: null,
    totalBerthHours: "",
    availableStockQuantity: "",
    vesselDischargeQuantity: "",
    comlete: "N",
    dischargeCargoStockPilr: "",
    bay: "",
    availableQuantity: "",
    dischargeOperationCommenced: null,
    dischargeOperationCompleted: null,
    berth: "",
    nor: null,
  });

  const [rakeData, setRakeData] = useState({
    remarks: "",
    equipmentShift: "",
    machinary: "",
    equipmentSupplier: "",
    noOfEquipments: "",
    equipmentStartTime: null,
    equipmentStopTime: null,
    equipmentTotalHours: "",
    equipmentRemarks: "",
    labourShift: "",
    labourSupplier: "",
    noOfPeople: "",
    labourStartTime: null,
    labourStopTime: null,
    labourTotalHours: "",
    labourRemarks: "",
    waterTankShift: "",
    waterTankSupplier: "",
    noOfWaterTanks: "",
    waterTankRemarks: "",
  });

  useEffect(() => {
    const fetchData = async () => {
      setIsLoading(true);
      try {
        const [
          partyRes,
          cargoRes,
          bayRes,
          berthRes,
          shiftRes,
          machRes,
          supplierRes,
          rakeRes,
        ] = await Promise.all([
          axiosInstance.get(`${API_CONFIG.BASE_URL}/menu/commercial/party`),
          axiosInstance.get(`${API_CONFIG.BASE_URL}/admin/commercial/cargo`),
          axiosInstance.get(`${API_CONFIG.BASE_URL}/admin/train/bay`),
          axiosInstance.get(
            `${API_CONFIG.BASE_URL}/menu/vesselOperations/getBerths`
          ),
          axiosInstance.get(`${API_CONFIG.BASE_URL}/admin/train/shift`),
          axiosInstance.get(
            `${API_CONFIG.BASE_URL}/admin/train/machinaryTypes`
          ),
          axiosInstance.get(`${API_CONFIG.BASE_URL}/admin/train/supplier`),
          axiosInstance.get(
            `${API_CONFIG.BASE_URL}/menu/vesselOperations/editVessel/${portOperationsId}`
          ),
        ]);

        setParties(normalizeDropdownData(partyRes.data));
        setCargoList(normalizeDropdownData(cargoRes.data));
        setBays(normalizeDropdownData(bayRes.data));
        setBerths(normalizeDropdownData(berthRes.data));
        setShifts(normalizeDropdownData(shiftRes.data));
        setMachineries(normalizeDropdownData(machRes.data));
        setSuppliers(normalizeDropdownData(supplierRes.data));

        const rakeResponse = rakeRes.data;
        setVesselDetails({
          vesselName: rakeResponse.vesselName || "",
          voyageNumber: rakeResponse.voyageNumber || "",
          cargo: rakeResponse.cargo || "",
          loadPort: rakeResponse.loadPort || "",
          dischargePort: rakeResponse.dischargePort || "",
          customer: rakeResponse.customer || "",
          contract: rakeResponse.contract || "",
          vesselLoadQuantity: rakeResponse.vesselLoadQuantity || "",
          berthingTime: rakeResponse.dischargeBerthingTime
            ? new Date(rakeResponse.dischargeBerthingTime)
            : null,
          sailingTime: rakeResponse.dischargeSailingTime
            ? new Date(rakeResponse.dischargeSailingTime)
            : null,
          totalBerthHours: rakeResponse.dischargeTotalBerthHours || "",
          availableStockQuantity: rakeResponse.dischargeAvlStockQty || "",
          vesselDischargeQuantity: rakeResponse.vesselDischargeQuantity || "",
          comlete: rakeResponse.comlete || "N",
          dischargeCargoStockPilr: rakeResponse.dischargeCargoStockPilr || "",
          bay: rakeResponse.dischargeBay || "",
          availableQuantity: rakeResponse.dischargeAvlQuantity || "",
          dischargeOperationCommenced: rakeResponse.dischargeOperationCommenced
            ? new Date(rakeResponse.dischargeOperationCommenced)
            : null,
          dischargeOperationCompleted: rakeResponse.dischargeOperationCompleted
            ? new Date(rakeResponse.dischargeOperationCompleted)
            : null,
          berth: rakeResponse.berth || "",
          nor: rakeResponse.nor ? new Date(rakeResponse.nor) : null,
        });

        setRakeData({
          remarks: rakeResponse.remarks || "",
          equipmentShift: rakeResponse.equipmentDetailsLoadDto?.shift || "",
          machinary: rakeResponse.equipmentDetailsLoadDto?.machinary || "",
          equipmentSupplier:
            rakeResponse.equipmentDetailsLoadDto?.supplier || "",
          noOfEquipments:
            rakeResponse.equipmentDetailsLoadDto?.noOfEquipments || "",
          equipmentStartTime: rakeResponse.equipmentDetailsLoadDto?.startTime
            ? new Date(rakeResponse.equipmentDetailsLoadDto.startTime)
            : null,
          equipmentStopTime: rakeResponse.equipmentDetailsLoadDto?.stopTime
            ? new Date(rakeResponse.equipmentDetailsLoadDto.stopTime)
            : null,
          equipmentTotalHours:
            rakeResponse.equipmentDetailsLoadDto?.totalHours || "",
          equipmentRemarks: rakeResponse.equipmentDetailsLoadDto?.remarks || "",
          labourShift: rakeResponse.labourDetailsLoadDto?.shift || "",
          labourSupplier: rakeResponse.labourDetailsLoadDto?.supplier || "",
          noOfPeople: rakeResponse.labourDetailsLoadDto?.noOfPeople || "",
          labourStartTime: rakeResponse.labourDetailsLoadDto?.startTime
            ? new Date(rakeResponse.labourDetailsLoadDto.startTime)
            : null,
          labourStopTime: rakeResponse.labourDetailsLoadDto?.stopTime
            ? new Date(rakeResponse.labourDetailsLoadDto.stopTime)
            : null,
          labourTotalHours: rakeResponse.labourDetailsLoadDto?.totalHours || "",
          labourRemarks: rakeResponse.labourDetailsLoadDto?.remarks || "",
          waterTankShift: rakeResponse.waterTankLoadDto?.shift || "",
          waterTankSupplier: rakeResponse.waterTankLoadDto?.supplier || "",
          noOfWaterTanks: rakeResponse.waterTankLoadDto?.noOfWaterTanks || "",
          waterTankRemarks: rakeResponse.waterTankLoadDto?.remarks || "",
        });

        const stockpileData = (rakeResponse.stockPileDetailsLoadDto || []).map(
          (item) => ({
            stockPileDetalId: item.stockPileDetalId || null,
            customer: item.customer || "",
            cargoStockPile: item.cargoStockPile || "",
            bay: item.bay || "",
            unloadedQuantity: item.unloadedQuantity || "",
            availableQuantity: item.availableQuantity || "",
            remarks: item.remarks || "",
          })
        );
        const uniqueStockpileData = Array.from(
          new Map(
            stockpileData.map((row) => [row.stockPileDetalId, row])
          ).values()
        );
        setStockpileRows(uniqueStockpileData);
      } catch (err) {
        alert("Failed to fetch discharge data!");
      } finally {
        setIsLoading(false);
      }
    };

    if (portOperationsId) {
      fetchData();
    }
  }, [portOperationsId]);

  const handleDetailsTabChange = (event, newValue) => {
    setDetailsTab(newValue);
  };

  const handleInputChange = (field, value) => {
    setRakeData((prev) => ({
      ...prev,
      [field]: value,
    }));
  };

  const handleVesselDetailsChange = (field, value) => {
    setVesselDetails((prev) => ({
      ...prev,
      [field]: value,
    }));
  };

  const handleStockpileChange = (index, field, value) => {
    setStockpileRows((prevRows) =>
      prevRows.map((row, i) => (i === index ? { ...row, [field]: value } : row))
    );
  };

  const addStockpileRow = () => {
    try {
      const newRow = {
        stockPileDetalId: null,
        customer: "",
        cargoStockPile: "",
        bay: "",
        unloadedQuantity: "",
        availableQuantity: "",
        remarks: "",
      };
      setStockpileRows([...stockpileRows, newRow]);
    } catch (error) {
      alert("Failed to add stockpile row!");
    }
  };

  const removeStockpileRow = (index) => {
    try {
      setStockpileRows(stockpileRows.filter((row, i) => i !== index));
      alert("Stockpile row removed successfully!");
    } catch (error) {
      alert("Failed to remove stockpile row!");
    }
  };

  const parseNumber = (value) => {
    if (value === "" || value === null || value === undefined) return null;
    const num = parseFloat(value);
    return isNaN(num) ? null : num;
  };

  const parseInteger = (value) => {
    if (value === "" || value === null || value === undefined) return null;
    const num = parseInt(value);
    return isNaN(num) ? null : num;
  };

  const handleSave = async () => {
    if (isLoading) return;

    if (
      !vesselDetails.vesselName ||
      !vesselDetails.customer ||
      !vesselDetails.cargo ||
      !vesselDetails.dischargePort ||
      !vesselDetails.loadPort
    ) {
      alert(
        "Please fill all required fields: Vessel Name, Customer, Cargo, Load Port, Discharge Port."
      );
      return;
    }

    setIsLoading(true);

    const dateFields = [
      { key: "berthingTime", value: vesselDetails.berthingTime },
      { key: "sailingTime", value: vesselDetails.sailingTime },
      {
        key: "dischargeOperationCommenced",
        value: vesselDetails.dischargeOperationCommenced,
      },
      {
        key: "dischargeOperationCompleted",
        value: vesselDetails.dischargeOperationCompleted,
      },
      { key: "equipmentStartTime", value: rakeData.equipmentStartTime },
      { key: "equipmentStopTime", value: rakeData.equipmentStopTime },
      { key: "labourStartTime", value: rakeData.labourStartTime },
      { key: "labourStopTime", value: rakeData.labourStopTime },
      { key: "nor", value: vesselDetails.nor },
    ];

    dateFields.forEach(({ key, value }) => {
      if (value && !(value instanceof Date)) {
        console.warn(`Invalid date for ${key}:`, value);
      }
    });

    const payload = {
      portOperationsId: parseInt(portOperationsId),
      vesselName: vesselDetails.vesselName || null,
      voyageNumber: vesselDetails.voyageNumber || null,
      cargo: vesselDetails.cargo || null,
      loadPort: vesselDetails.loadPort || null,
      dischargePort: vesselDetails.dischargePort || null,
      customer: vesselDetails.customer || null,
      contract: vesselDetails.contract || null,
      vesselLoadQuantity: parseNumber(vesselDetails.vesselLoadQuantity),
      remarks: rakeData.remarks || null,
      dischargeOperationCommenced: vesselDetails.dischargeOperationCommenced
        ? format(
            vesselDetails.dischargeOperationCommenced,
            "yyyy-MM-dd HH:mm:ss"
          )
        : null,
      dischargeOperationCompleted: vesselDetails.dischargeOperationCompleted
        ? format(
            vesselDetails.dischargeOperationCompleted,
            "yyyy-MM-dd HH:mm:ss"
          )
        : null,
      vesselDischargeQuantity: parseNumber(
        vesselDetails.vesselDischargeQuantity
      ),
      comlete: vesselDetails.comlete || null,
      dischargeBerthingTime: vesselDetails.berthingTime
        ? format(vesselDetails.berthingTime, "yyyy-MM-dd HH:mm:ss")
        : null,
      dischargeSailingTime: vesselDetails.sailingTime
        ? format(vesselDetails.sailingTime, "yyyy-MM-dd HH:mm:ss")
        : null,
      dischargeTotalBerthHours: parseInteger(vesselDetails.totalBerthHours),
      dischargeCargoStockPilr: vesselDetails.dischargeCargoStockPilr || null,
      dischargeBay: vesselDetails.bay || null,
      dischargeAvlStockQty: parseNumber(vesselDetails.availableStockQuantity),
      dischargeAvlQuantity: parseNumber(vesselDetails.availableQuantity),
      berth: vesselDetails.berth || null,
      nor: vesselDetails.nor
        ? format(vesselDetails.nor, "yyyy-MM-dd HH:mm:ss")
        : null,
      stockPileDetailsLoadDto: stockpileRows.map((row) => ({
        stockPileDetalId: row.stockPileDetalId || null,
        customer: row.customer || null,
        cargoStockPile: row.cargoStockPile || null,
        bay: row.bay || null,
        unloadedQuantity: parseNumber(row.unloadedQuantity),
        availableQuantity: parseNumber(row.availableQuantity),
        remarks: row.remarks || null,
      })),
      equipmentDetailsLoadDto: {
        shift: rakeData.equipmentShift || null,
        machinary: rakeData.machinary || null,
        supplier: rakeData.equipmentSupplier || null,
        noOfEquipments: parseInteger(rakeData.noOfEquipments),
        startTime: rakeData.equipmentStartTime
          ? format(rakeData.equipmentStartTime, "yyyy-MM-dd HH:mm:ss")
          : null,
        stopTime: rakeData.equipmentStopTime
          ? format(rakeData.equipmentStopTime, "yyyy-MM-dd HH:mm:ss")
          : null,
        totalHours: parseInteger(rakeData.equipmentTotalHours),
        remarks: rakeData.equipmentRemarks || null,
      },
      labourDetailsLoadDto: {
        shift: rakeData.labourShift || null,
        supplier: rakeData.labourSupplier || null,
        noOfPeople: parseInteger(rakeData.noOfPeople),
        startTime: rakeData.labourStartTime
          ? format(rakeData.labourStartTime, "yyyy-MM-dd HH:mm:ss")
          : null,
        stopTime: rakeData.labourStopTime
          ? format(rakeData.labourStopTime, "yyyy-MM-dd HH:mm:ss")
          : null,
        totalHours: parseInteger(rakeData.labourTotalHours),
        remarks: rakeData.labourRemarks || null,
      },
      waterTankLoadDto: {
        shift: rakeData.waterTankShift || null,
        supplier: rakeData.waterTankSupplier || null,
        noOfWaterTanks: parseInteger(rakeData.noOfWaterTanks),
        remarks: rakeData.waterTankRemarks || null,
      },
    };

    console.log("Payload sent to backend:", JSON.stringify(payload, null, 2));

    try {
      const response = await axiosInstance.post(
        `${API_CONFIG.BASE_URL}/menu/vesselOperations/saveVesselLoadOperation`,
        payload
      );
      alert("Data saved successfully!");
      navigate(-1);
    } catch (error) {
      const errorMessage =
        error.response?.status === 400
          ? "Invalid data provided. Please check your inputs."
          : error.response?.status === 500
          ? "Server error. Please try again later."
          : error.response?.data?.message || error.message;
      alert(`Failed to save discharge data: ${errorMessage}`);
    } finally {
      setIsLoading(false);
    }
  };

  const handleCancel = () => {
    navigate(-1);
  };

  const tabItems = [
    { id: 0, label: "Vessel Delay", icon: <TrainIcon /> },
    { id: 1, label: "Discharge Handling", icon: <LocalShippingIcon /> },
    { id: 2, label: "Stockpile Details", icon: <InventoryIcon /> },
    { id: 3, label: "Equipment Details", icon: <BuildIcon /> },
    { id: 4, label: "Labour Details", icon: <PeopleIcon /> },
    { id: 5, label: "Water Tank Details", icon: <WaterIcon /> },
  ];

  return (
    <LocalizationProvider dateAdapter={AdapterDateFns}>
      <Box sx={{ p: 3, maxWidth: "1200px", mx: "auto" }}>
        <Card
          sx={{
            display: "flex",
            justifyContent: "space-between",
            alignItems: "center",
            mb: 3,
            px: 2,
            py: 1,
            backgroundColor: "white",
            boxShadow: 3,
            borderRadius: 2,
          }}
        >
          <Typography
            variant="h6"
            color="text.primary"
            sx={{ fontWeight: 700 }}
          >
            Vessel Discharge Details
          </Typography>
          <IconButton color="inherit" onClick={() => navigate(-1)}>
            <CancelIcon />
          </IconButton>
        </Card>

        {isLoading && (
          <Box sx={{ display: "flex", justifyContent: "center", my: 4 }}>
            <CircularProgress />
          </Box>
        )}

        {/* Vessel Details */}
        <Card sx={{ mb: 3, p: 3, boxShadow: 2, borderRadius: 2 }}>
          <Grid container spacing={2}>
            <Grid item xs={12} sm={6} md={3}>
              <TextField
                fullWidth
                label="Contract No"
                value={vesselDetails.contract || ""}
                onChange={(e) =>
                  handleVesselDetailsChange("contract", e.target.value)
                }
                variant="outlined"
              />
            </Grid>
            <Grid item xs={12} sm={6} md={3}>
              <TextField
                fullWidth
                label="Vessel Name"
                value={vesselDetails.vesselName || ""}
                onChange={(e) =>
                  handleVesselDetailsChange("vesselName", e.target.value)
                }
                variant="outlined"
                required
              />
            </Grid>
            <Grid item xs={12} sm={6} md={3}>
              <TextField
                fullWidth
                label="Voyage No."
                value={vesselDetails.voyageNumber || ""}
                onChange={(e) =>
                  handleVesselDetailsChange("voyageNumber", e.target.value)
                }
                variant="outlined"
              />
            </Grid>
            <Grid item xs={12} sm={6} md={3}>
              <FormControl fullWidth variant="outlined">
                <InputLabel>Cargo</InputLabel>
                <Select
                  value={vesselDetails.cargo || ""}
                  onChange={(e) =>
                    handleVesselDetailsChange("cargo", e.target.value)
                  }
                  label="Cargo"
                  required
                >
                  <MenuItem value="" disabled>
                    SELECT CARGO
                  </MenuItem>
                  {cargoList.map((cargo) => (
                    <MenuItem key={cargo.id} value={cargo.name}>
                      {cargo.name}
                    </MenuItem>
                  ))}
                </Select>
              </FormControl>
            </Grid>
            <Grid item xs={12} sm={6} md={3}>
              <TextField
                fullWidth
                label="Load Port"
                value={vesselDetails.loadPort || ""}
                onChange={(e) =>
                  handleVesselDetailsChange("loadPort", e.target.value)
                }
                variant="outlined"
                required
              />
            </Grid>
            <Grid item xs={12} sm={6} md={3}>
              <TextField
                fullWidth
                label="Discharge Port"
                value={vesselDetails.dischargePort || ""}
                onChange={(e) =>
                  handleVesselDetailsChange("dischargePort", e.target.value)
                }
                variant="outlined"
                required
              />
            </Grid>
            <Grid item xs={12} sm={6} md={3}>
              <FormControl fullWidth variant="outlined">
                <InputLabel>Customer</InputLabel>
                <Select
                  value={vesselDetails.customer || ""}
                  onChange={(e) =>
                    handleVesselDetailsChange("customer", e.target.value)
                  }
                  label="Customer"
                  required
                >
                  <MenuItem value="" disabled>
                    SELECT CUSTOMER
                  </MenuItem>
                  {parties.map((party) => (
                    <MenuItem key={party.id} value={party.name}>
                      {party.name}
                    </MenuItem>
                  ))}
                </Select>
              </FormControl>
            </Grid>
            <Grid item xs={12} sm={6} md={3}>
              <FormControl fullWidth variant="outlined">
                <InputLabel>Berth</InputLabel>
                <Select
                  value={vesselDetails.berth || ""}
                  onChange={(e) =>
                    handleVesselDetailsChange("berth", e.target.value)
                  }
                  label="Berth"
                >
                  <MenuItem value="" disabled>
                    SELECT BERTH
                  </MenuItem>
                  {berths.map((berth) => (
                    <MenuItem key={berth.id} value={berth.name}>
                      {berth.name}
                    </MenuItem>
                  ))}
                </Select>
              </FormControl>
            </Grid>
            <Grid item xs={12} sm={6} md={3}>
              <DateTimePicker
                label="NOR"
                value={vesselDetails.nor || null}
                onChange={(newValue) =>
                  handleVesselDetailsChange("nor", newValue)
                }
                ampm={false}
                format="yyyy-MM-dd HH:mm:ss"
                slotProps={{
                  textField: { variant: "outlined", fullWidth: true },
                }}
              />
            </Grid>
            <Grid item xs={12} sm={6} md={3}>
              <TextField
                fullWidth
                label="Vessel Load Quantity (MT)"
                type="number"
                value={vesselDetails.vesselLoadQuantity || ""}
                onChange={(e) =>
                  handleVesselDetailsChange(
                    "vesselLoadQuantity",
                    e.target.value
                  )
                }
                inputProps={{ min: 0, step: "0.01" }}
                variant="outlined"
              />
            </Grid>
            <Grid item xs={12} sm={6} md={3}>
              <TextField
                fullWidth
                label="Vessel Discharge Quantity (MT)"
                type="number"
                value={vesselDetails.vesselDischargeQuantity || ""}
                onChange={(e) =>
                  handleVesselDetailsChange(
                    "vesselDischargeQuantity",
                    e.target.value
                  )
                }
                inputProps={{ min: 0, step: "0.01" }}
                variant="outlined"
              />
            </Grid>
            {/* <Grid item xs={12} sm={6} md={3}>
              <TextField
                fullWidth
                disabled
                label="Discharge Cargo Stockpile"
                value={vesselDetails.dischargeCargoStockPilr || ""}
                onChange={(e) => handleVesselDetailsChange("dischargeCargoStockPilr", e.target.value)}
                variant="outlined"
              />
            </Grid>
            <Grid item xs={12} sm={6} md={3}>
              <FormControl fullWidth variant="outlined">
                <InputLabel>Discharge Bay</InputLabel>
                <Select
                  disabled
                  value={vesselDetails.bay || ""}
                  onChange={(e) => handleVesselDetailsChange("bay", e.target.value)}
                  label="Discharge Bay"
                >
                  <MenuItem value="">
                    SELECT BAY
                  </MenuItem>
                  {bays.map((bay) => (
                    <MenuItem key={bay.id} value={bay.name}>
                      {bay.name}
                    </MenuItem>
                  ))}
                </Select>
              </FormControl>
            </Grid>
            <Grid item xs={12} sm={6} md={3}>
              <TextField
                fullWidth
                disabled
                label="Available Stock Quantity (MT)"
                type="number"
                value={vesselDetails.availableStockQuantity || ""}
                onChange={(e) => handleVesselDetailsChange("availableStockQuantity", e.target.value)}
                inputProps={{ min: 0, step: "0.01" }}
                variant="outlined"
              />
            </Grid>
            <Grid item xs={12} sm={6} md={3}>
              <TextField
                fullWidth
                label="Available Quantity (MT)"
                type="number"
                value={vesselDetails.availableQuantity || ""}
                onChange={(e) => handleVesselDetailsChange("availableQuantity", e.target.value)}
                inputProps={{ min: 0, step: "0.01" }}
                variant="outlined"
              />
            </Grid> */}
          </Grid>
        </Card>

        <Card sx={{ p: 3, boxShadow: 2, borderRadius: 2 }}>
          <Tabs
            value={detailsTab}
            onChange={handleDetailsTabChange}
            variant="scrollable"
            scrollButtons="auto"
            sx={{
              backgroundColor: "#222943",
              padding: "6px",
              "& .MuiTab-root": {
                color: "#e0e0e0", // softer gray for text
                transition: "all 0.3s ease",
                minHeight: "60px",
                px: 2,
                mx: 0.5,
                fontWeight: 500,
                "& svg": {
                  color: "#eef5f6ff", // 👈 accent color for icons (cyan)
                  marginRight: "8px",
                  transition: "all 0.3s ease",
                },
                "&:hover": {
                  backgroundColor: "rgba(255, 255, 255, 0.08)",
                  color: "#ffffff",
                  "& svg": { color: "#ffffff" }, // icon turns white on hover
                },
              },
            }}
          >
            {tabItems.map((item) => (
              <Tab
                key={item.id}
                label={item.label}
                icon={item.icon}
                iconPosition="start"
              />
            ))}
          </Tabs>

          <CardContent>
            {isLoading && (
              <Box sx={{ display: "flex", justifyContent: "center", my: 4 }}>
                <CircularProgress />
              </Box>
            )}

            {!isLoading && detailsTab === 0 && (
              <Box>
                <Grid container spacing={2}>
                  <Grid item xs={12} sm={6} md={3}>
                    <TextField
                      fullWidth
                      label="Remarks"
                      value={rakeData.remarks || ""}
                      onChange={(e) =>
                        handleInputChange("remarks", e.target.value)
                      }
                      multiline
                      rows={1}
                      variant="outlined"
                    />
                  </Grid>





                  
                </Grid>
              </Box>
            )}

            {!isLoading && detailsTab === 1 && (
              <Box>
                <Grid container spacing={2}>
                  <Grid item xs={12} sm={6} md={3}>
                    <DateTimePicker
                      label="Discharge Berthing Time"
                      value={vesselDetails.berthingTime}
                      onChange={(newValue) =>
                        handleVesselDetailsChange("berthingTime", newValue)
                      }
                      format="yyyy-MM-dd HH:mm:ss"
                      ampm={false}
                      slotProps={{
                        textField: { variant: "outlined", fullWidth: true },
                      }}
                    />
                  </Grid>
                  <Grid item xs={12} sm={6} md={3}>
                    <DateTimePicker
                      label="Discharge Operation Commenced"
                      value={vesselDetails.dischargeOperationCommenced}
                      onChange={(newValue) =>
                        handleVesselDetailsChange(
                          "dischargeOperationCommenced",
                          newValue
                        )
                      }
                      format="yyyy-MM-dd HH:mm:ss"
                      ampm={false}
                      slotProps={{
                        textField: { variant: "outlined", fullWidth: true },
                      }}
                    />
                  </Grid>
                  <Grid item xs={12} sm={6} md={3}>
                    <DateTimePicker
                      label="Discharge Operation Completed"
                      value={vesselDetails.dischargeOperationCompleted}
                      onChange={(newValue) =>
                        handleVesselDetailsChange(
                          "dischargeOperationCompleted",
                          newValue
                        )
                      }
                      format="yyyy-MM-dd HH:mm:ss"
                      ampm={false}
                      slotProps={{
                        textField: { variant: "outlined", fullWidth: true },
                      }}
                    />
                  </Grid>
                  <Grid item xs={12} sm={6} md={3}>
                    <DateTimePicker
                      label="Discharge Sailing Time"
                      value={vesselDetails.sailingTime}
                      onChange={(newValue) =>
                        handleVesselDetailsChange("sailingTime", newValue)
                      }
                      format="yyyy-MM-dd HH:mm:ss"
                      ampm={false}
                      slotProps={{
                        textField: { variant: "outlined", fullWidth: true },
                      }}
                    />
                  </Grid>
                  <Grid item xs={12} sm={6} md={3}>
                    <TextField
                      fullWidth
                      disabled
                      label="Discharge Total Berth Hours"
                      type="number"
                      value={vesselDetails.totalBerthHours || ""}
                      onChange={(e) =>
                        handleVesselDetailsChange(
                          "totalBerthHours",
                          e.target.value
                        )
                      }
                      inputProps={{ min: 0 }}
                      variant="outlined"
                    />
                  </Grid>
                </Grid>
              </Box>
            )}

            {!isLoading && detailsTab === 2 && (
              <Box>
                <TableContainer component={Paper} sx={{ mb: 2 }}>
                  <Table>
                    <TableHead>
                      <TableRow>
                        <TableCell>Customer</TableCell>
                        <TableCell>Cargo Stockpile</TableCell>
                        <TableCell>Bay</TableCell>
                        <TableCell>Unloaded Quantity</TableCell>
                        <TableCell>Available Quantity</TableCell>
                        <TableCell>Remarks</TableCell>
                        <TableCell>Action</TableCell>
                      </TableRow>
                    </TableHead>
                    <TableBody>
                      {stockpileRows.map((row, index) => (
                        <TableRow key={index}>
                          <TableCell>
                            <FormControl fullWidth variant="outlined">
                              <InputLabel>Customer</InputLabel>
                              <Select
                                label="Customer"
                                value={row.customer || ""}
                                onChange={(e) =>
                                  handleStockpileChange(
                                    index,
                                    "customer",
                                    e.target.value
                                  )
                                }
                              >
                                <MenuItem value="" disabled>
                                  SELECT CUSTOMER
                                </MenuItem>
                                {parties.map((party) => (
                                  <MenuItem key={party.id} value={party.name}>
                                    {party.name}
                                  </MenuItem>
                                ))}
                              </Select>
                            </FormControl>
                          </TableCell>
                          <TableCell>
                            <FormControl fullWidth variant="outlined">
                              <InputLabel>Cargo Stockpile</InputLabel>
                              <Select
                                label="Cargo Stockpile"
                                value={row.cargoStockPile || ""}
                                onChange={(e) =>
                                  handleStockpileChange(
                                    index,
                                    "cargoStockPile",
                                    e.target.value
                                  )
                                }
                              >
                                <MenuItem value="" disabled>
                                  SELECT CARGO
                                </MenuItem>
                                {cargoList.map((cargo) => (
                                  <MenuItem key={cargo.id} value={cargo.name}>
                                    {cargo.name}
                                  </MenuItem>
                                ))}
                              </Select>
                            </FormControl>
                          </TableCell>
                          <TableCell>
                            <FormControl fullWidth variant="outlined">
                              <InputLabel>Bay</InputLabel>
                              <Select
                                label="Bay"
                                value={row.bay || ""}
                                onChange={(e) =>
                                  handleStockpileChange(
                                    index,
                                    "bay",
                                    e.target.value
                                  )
                                }
                              >
                                <MenuItem value="" disabled>
                                  SELECT BAY
                                </MenuItem>
                                {bays.map((bay) => (
                                  <MenuItem key={bay.id} value={bay.name}>
                                    {bay.name}
                                  </MenuItem>
                                ))}
                              </Select>
                            </FormControl>
                          </TableCell>
                          <TableCell>
                            <TextField
                              fullWidth
                              type="number"
                              value={row.unloadedQuantity || ""}
                              onChange={(e) =>
                                handleStockpileChange(
                                  index,
                                  "unloadedQuantity",
                                  e.target.value
                                )
                              }
                              inputProps={{ min: 0, step: "0.01" }}
                              variant="outlined"
                            />
                          </TableCell>
                          <TableCell>
                            <TextField
                              fullWidth
                              type="number"
                              value={row.availableQuantity || ""}
                              onChange={(e) =>
                                handleStockpileChange(
                                  index,
                                  "availableQuantity",
                                  e.target.value
                                )
                              }
                              inputProps={{ min: 0, step: "0.01" }}
                              variant="outlined"
                            />
                          </TableCell>
                          <TableCell>
                            <TextField
                              fullWidth
                              value={row.remarks || ""}
                              onChange={(e) =>
                                handleStockpileChange(
                                  index,
                                  "remarks",
                                  e.target.value
                                )
                              }
                              variant="outlined"
                            />
                          </TableCell>
                          <TableCell>
                            <Button
                              variant="contained"
                              color="error"
                              onClick={() => removeStockpileRow(index)}
                            >
                              Delete
                            </Button>
                          </TableCell>
                        </TableRow>
                      ))}
                    </TableBody>
                  </Table>
                </TableContainer>
                <Button
                  variant="contained"
                  color="primary"
                  sx={{ mt: 2 }}
                  onClick={addStockpileRow}
                >
                  + Add Row
                </Button>
              </Box>
            )}

            {!isLoading && detailsTab === 3 && (
              <Box>
                <Grid container spacing={2}>
                  <Grid item xs={12} sm={6} md={3}>
                    <FormControl fullWidth variant="outlined">
                      <InputLabel>Shift</InputLabel>
                      <Select
                        label="Shift"
                        value={rakeData.equipmentShift || ""}
                        onChange={(e) =>
                          handleInputChange("equipmentShift", e.target.value)
                        }
                      >
                        <MenuItem value="" disabled>
                          SELECT SHIFT
                        </MenuItem>
                        {shifts.map((shift) => (
                          <MenuItem key={shift.id} value={shift.name}>
                            {shift.name}
                          </MenuItem>
                        ))}
                      </Select>
                    </FormControl>
                  </Grid>
                  <Grid item xs={12} sm={6} md={3}>
                    <FormControl fullWidth variant="outlined">
                      <InputLabel>Machinery</InputLabel>
                      <Select
                        label="Machinery"
                        value={rakeData.machinary || ""}
                        onChange={(e) =>
                          handleInputChange("machinary", e.target.value)
                        }
                      >
                        <MenuItem value="" disabled>
                          SELECT MACHINERY
                        </MenuItem>
                        {machineries.map((m) => (
                          <MenuItem key={m.id} value={m.name}>
                            {m.name}
                          </MenuItem>
                        ))}
                      </Select>
                    </FormControl>
                  </Grid>
                  <Grid item xs={12} sm={6} md={3}>
                    <FormControl fullWidth variant="outlined">
                      <InputLabel>Supplier</InputLabel>
                      <Select
                        label="Supplier"
                        value={rakeData.equipmentSupplier || ""}
                        onChange={(e) =>
                          handleInputChange("equipmentSupplier", e.target.value)
                        }
                      >
                        <MenuItem value="" disabled>
                          SELECT SUPPLIER
                        </MenuItem>
                        {suppliers.map((s) => (
                          <MenuItem key={s.id} value={s.name}>
                            {s.name}
                          </MenuItem>
                        ))}
                      </Select>
                    </FormControl>
                  </Grid>
                  <Grid item xs={12} sm={6} md={3}>
                    <TextField
                      fullWidth
                      label="No. of Equipments"
                      type="number"
                      value={rakeData.noOfEquipments || ""}
                      onChange={(e) =>
                        handleInputChange("noOfEquipments", e.target.value)
                      }
                      inputProps={{ min: 0 }}
                      variant="outlined"
                    />
                  </Grid>
                  <Grid item xs={12} sm={6} md={3}>
                    <DateTimePicker
                      label="Start Time"
                      value={rakeData.equipmentStartTime}
                      onChange={(newValue) =>
                        handleInputChange("equipmentStartTime", newValue)
                      }
                      format="yyyy-MM-dd HH:mm:ss"
                      ampm={false}
                      slotProps={{
                        textField: { variant: "outlined", fullWidth: true },
                      }}
                    />
                  </Grid>
                  <Grid item xs={12} sm={6} md={3}>
                    <DateTimePicker
                      label="Stop Time"
                      value={rakeData.equipmentStopTime}
                      onChange={(newValue) =>
                        handleInputChange("equipmentStopTime", newValue)
                      }
                      format="yyyy-MM-dd HH:mm:ss"
                      ampm={false}
                      slotProps={{
                        textField: { variant: "outlined", fullWidth: true },
                      }}
                    />
                  </Grid>
                  <Grid item xs={12} sm={6} md={3}>
                    <TextField
                      fullWidth
                      label="Total Hours"
                      type="number"
                      value={rakeData.equipmentTotalHours || ""}
                      onChange={(e) =>
                        handleInputChange("equipmentTotalHours", e.target.value)
                      }
                      inputProps={{ min: 0 }}
                      variant="outlined"
                    />
                  </Grid>
                  <Grid item xs={12} sm={6} md={3}>
                    <TextField
                      fullWidth
                      label="Remarks"
                      value={rakeData.equipmentRemarks || ""}
                      onChange={(e) =>
                        handleInputChange("equipmentRemarks", e.target.value)
                      }
                      multiline
                      rows={1}
                      variant="outlined"
                    />
                  </Grid>
                </Grid>
              </Box>
            )}

            {!isLoading && detailsTab === 4 && (
              <Box>
                <Grid container spacing={2}>
                  <Grid item xs={12} sm={6} md={3}>
                    <FormControl fullWidth variant="outlined">
                      <InputLabel>Shift</InputLabel>
                      <Select
                        label="Shift"
                        value={rakeData.labourShift || ""}
                        onChange={(e) =>
                          handleInputChange("labourShift", e.target.value)
                        }
                      >
                        <MenuItem value="" disabled>
                          SELECT SHIFT
                        </MenuItem>
                        {shifts.map((shift) => (
                          <MenuItem key={shift.id} value={shift.name}>
                            {shift.name}
                          </MenuItem>
                        ))}
                      </Select>
                    </FormControl>
                  </Grid>
                  <Grid item xs={12} sm={6} md={3}>
                    <FormControl fullWidth variant="outlined">
                      <InputLabel>Supplier</InputLabel>
                      <Select
                        label="Supplier"
                        value={rakeData.labourSupplier || ""}
                        onChange={(e) =>
                          handleInputChange("labourSupplier", e.target.value)
                        }
                      >
                        <MenuItem value="" disabled>
                          SELECT SUPPLIER
                        </MenuItem>
                        {suppliers.map((s) => (
                          <MenuItem key={s.id} value={s.name}>
                            {s.name}
                          </MenuItem>
                        ))}
                      </Select>
                    </FormControl>
                  </Grid>
                  <Grid item xs={12} sm={6} md={3}>
                    <TextField
                      fullWidth
                      label="No. of People"
                      type="number"
                      value={rakeData.noOfPeople || ""}
                      onChange={(e) =>
                        handleInputChange("noOfPeople", e.target.value)
                      }
                      inputProps={{ min: 0 }}
                      variant="outlined"
                    />
                  </Grid>
                  <Grid item xs={12} sm={6} md={3}>
                    <DateTimePicker
                      label="Start Time"
                      value={rakeData.labourStartTime}
                      onChange={(newValue) =>
                        handleInputChange("labourStartTime", newValue)
                      }
                      format="yyyy-MM-dd HH:mm:ss"
                      ampm={false}
                      slotProps={{
                        textField: { variant: "outlined", fullWidth: true },
                      }}
                    />
                  </Grid>
                  <Grid item xs={12} sm={6} md={3}>
                    <DateTimePicker
                      label="Stop Time"
                      value={rakeData.labourStopTime}
                      onChange={(newValue) =>
                        handleInputChange("labourStopTime", newValue)
                      }
                      format="yyyy-MM-dd HH:mm:ss"
                      ampm={false}
                      slotProps={{
                        textField: { variant: "outlined", fullWidth: true },
                      }}
                    />
                  </Grid>
                  <Grid item xs={12} sm={6} md={3}>
                    <TextField
                      fullWidth
                      label="Total Hours"
                      type="number"
                      value={rakeData.labourTotalHours || ""}
                      onChange={(e) =>
                        handleInputChange("labourTotalHours", e.target.value)
                      }
                      inputProps={{ min: 0 }}
                      variant="outlined"
                    />
                  </Grid>
                  <Grid item xs={12} sm={6} md={3}>
                    <TextField
                      fullWidth
                      label="Remarks"
                      value={rakeData.labourRemarks || ""}
                      onChange={(e) =>
                        handleInputChange("labourRemarks", e.target.value)
                      }
                      multiline
                      rows={1}
                      variant="outlined"
                    />
                  </Grid>
                </Grid>
              </Box>
            )}

            {!isLoading && detailsTab === 5 && (
              <Box>
                <Grid container spacing={2}>
                  <Grid item xs={12} sm={6} md={3}>
                    <FormControl fullWidth variant="outlined">
                      <InputLabel>Shift</InputLabel>
                      <Select
                        label="Shift"
                        value={rakeData.waterTankShift || ""}
                        onChange={(e) =>
                          handleInputChange("waterTankShift", e.target.value)
                        }
                      >
                        <MenuItem value="" disabled>
                          SELECT SHIFT
                        </MenuItem>
                        {shifts.map((shift) => (
                          <MenuItem key={shift.id} value={shift.name}>
                            {shift.name}
                          </MenuItem>
                        ))}
                      </Select>
                    </FormControl>
                  </Grid>
                  <Grid item xs={12} sm={6} md={3}>
                    <FormControl fullWidth variant="outlined">
                      <InputLabel>Supplier</InputLabel>
                      <Select
                        label="Supplier"
                        value={rakeData.waterTankSupplier || ""}
                        onChange={(e) =>
                          handleInputChange("waterTankSupplier", e.target.value)
                        }
                      >
                        <MenuItem value="" disabled>
                          SELECT SUPPLIER
                        </MenuItem>
                        {suppliers.map((s) => (
                          <MenuItem key={s.id} value={s.name}>
                            {s.name}
                          </MenuItem>
                        ))}
                      </Select>
                    </FormControl>
                  </Grid>
                  <Grid item xs={12} sm={6} md={3}>
                    <TextField
                      fullWidth
                      label="No. of Water Tanks"
                      type="number"
                      value={rakeData.noOfWaterTanks || ""}
                      onChange={(e) =>
                        handleInputChange("noOfWaterTanks", e.target.value)
                      }
                      inputProps={{ min: 0 }}
                      variant="outlined"
                    />
                  </Grid>
                  <Grid item xs={12} sm={6} md={3}>
                    <TextField
                      fullWidth
                      label="Remarks"
                      value={rakeData.waterTankRemarks || ""}
                      onChange={(e) =>
                        handleInputChange("waterTankRemarks", e.target.value)
                      }
                      multiline
                      rows={1}
                      variant="outlined"
                    />
                  </Grid>
                </Grid>
              </Box>
            )}
          </CardContent>

          <Box
            sx={{
              position: "fixed",
              right: 20,
              bottom: 20,
              display: "flex",
              gap: 2,
              zIndex: 1300,
            }}
          >
            <Button
              variant="contained"
              color="primary"
              size="large"
              startIcon={<SaveIcon />}
              onClick={handleSave}
              disabled={isLoading}
            >
              Save
            </Button>
            <Button
              variant="contained"
              color="error"
              size="large"
              startIcon={<CancelIcon />}
              onClick={handleCancel}
              disabled={isLoading}
            >
              Cancel
            </Button>
          </Box>
        </Card>
      </Box>
    </LocalizationProvider>
  );
};

export default Discharge;
